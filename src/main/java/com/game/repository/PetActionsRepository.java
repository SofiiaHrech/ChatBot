package com.game.repository;

import com.game.model.PetActions;
import org.springframework.data.jpa.repository.JpaRepository;

public interface     PetActionsRepository extends JpaRepository<PetActions, Long> {
}
